<aside class="gm-limitations not-prose" aria-labelledby="gm-limitations-title">
  <p class="gm-limitations-eyebrow">Interpretation boundary</p>
  <h3 id="gm-limitations-title">Limitations</h3>

  <ul>
    <li>The exercise used simulated rather than real capital.</li>
    <li>
      Two short-duration sessions provide a useful process comparison but do not
      constitute a repeatable performance record.
    </li>
    <li>
      Several portal metrics and the composite weighting formula are proprietary
      or incompletely defined.
    </li>
    <li>
      Absolute P&amp;L is not directly comparable with audited real-portfolio
      performance.
    </li>
    <li>
      The event page reports 153 participants, while a manual ranked-list count
      produced 148; the asset-management P&amp;L rank is therefore published
      without a denominator.
    </li>
    <li>
      The 15 July cohort size is not documented, so its composite rank is shown
      without a denominator and is not compared mechanically with the later
      cohort.
    </li>
    <li>
      No transaction-level dataset is available for an independent
      reconstruction of either portfolio or the dealer book.
    </li>
  </ul>
</aside>

<style>
  .gm-limitations {
    margin: 1.5rem 0 2.2rem;
    padding: 1.2rem 1.25rem 1.15rem;
    border: 1px solid color-mix(in srgb, currentColor 15%, transparent);
    border-left: 3px solid #89b9d9;
    border-radius: 0.5rem;
    background: color-mix(in srgb, currentColor 3%, transparent);
  }

  .gm-limitations-eyebrow {
    margin: 0 0 0.4rem;
    font-size: 0.64rem;
    font-weight: 650;
    line-height: 1.3;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    opacity: 0.46;
  }

  .gm-limitations h3 {
    margin: 0;
    font-family: Georgia, Cambria, 'Times New Roman', Times, serif;
    font-size: 1.3rem;
    font-weight: 500;
    line-height: 1.3;
  }

  .gm-limitations ul {
    display: grid;
    gap: 0.65rem;
    margin: 1rem 0 0;
    padding: 0;
    list-style: none;
  }

  .gm-limitations li {
    position: relative;
    margin: 0;
    padding-left: 1rem;
    font-size: 0.82rem;
    line-height: 1.55;
    opacity: 0.68;
  }

  .gm-limitations li::before {
    position: absolute;
    top: 0.64em;
    left: 0;
    width: 0.3rem;
    height: 0.3rem;
    border-radius: 999px;
    background: currentColor;
    content: '';
    opacity: 0.42;
  }
</style>
